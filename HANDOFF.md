# 오웬이 해주실 것 — 2026-09-25 기준

순서대로 하시면 됩니다. **①②는 5분**, ③은 안 하셔도 앱은 돕니다.

---

## ① 지금 막혀 있는 것 — 6기 인증 암호 심기 ⚠️

**이거 안 하면 6기로 가입이 안 됩니다.** (지금 `verify_cohort` → HTTP 404)

Supabase › SQL Editor 에서 **`supabase-cohort.sql`** 실행.
파일 안의 `'여기에_학교_암호'` 를 **실제 암호(4416*)로 바꿔서** 실행하세요.

암호는 bcrypt 해시로만 저장되고, 앱 코드·리포 어디에도 안 들어갑니다.
실행 후 확인:
```sql
select verify_cohort('6기', '실제암호');   -- true 나와야 함
```

> 지금은 6기 가입 시 "학교 인증이 아직 준비되지 않았어요"가 뜨고 **계정이 안 만들어집니다.**
> 의도한 동작입니다 — 확인 불가를 통과로 치면 안 되니까요.

## ② 청소 + 반응 기능 켜기

Supabase › SQL Editor 에서 두 파일 실행:

| 파일 | 하는 일 |
|---|---|
| **`supabase-cleanup.sql`** | 마을채팅 15개 비우기 + **테스트 계정 27개 삭제** |
| **`supabase-reacts.sql`** | 👏🔥🌊💪 반응 테이블 만들기 |

테스트 계정이 27개로 늘어난 건 제 QA가 로그인할 때마다 행을 만들어서입니다.
**원인은 막았습니다**(하네스 37개에 쓰기 차단) — 앞으로는 안 늘어납니다.
지금 서버엔 진짜 3명(infinagree·owen·suna)과 테스트 27개가 섞여 있습니다.
앱에서는 `zz*`가 숨겨져 안 보이지만, 지우는 게 깔끔합니다.

반응 테이블을 안 만들면 **반응 칩이 아예 안 뜹니다**(죽은 버튼은 안 남깁니다).

## ③ 카카오 로그인 — 하실 거면

**`KAKAO.md`** 에 ①~⑥ 단계별로 적어뒀습니다. 30분쯤 걸립니다.

핵심 세 가지만:
- 개발자 앱 등록은 **오웬 카카오 계정으로만** 가능해서 제가 못 합니다
- **이메일은 사업자등록(Biz App)이 있어야** 받습니다. 없으면 닉네임+프로필사진만 — 서핑 앱엔 충분합니다
- Redirect URI는 surf.owenai.xyz가 아니라 **`https://tneidzyhriefzubkajkv.supabase.co/auth/v1/callback`**

**REST API 키·Client Secret은 저에게 보내지 마세요.** Supabase 콘솔에 직접 넣으시면 됩니다.

앱 심사에 필요한 URL은 이미 만들어 뒀습니다:
```
https://surf.owenai.xyz/terms-of-service.html
https://surf.owenai.xyz/privacy.html
```

⑥까지 끝나고 알려주시면 제가 "카카오로 시작하기" 버튼을 붙입니다.
기존 20명 계정은 **건드리지 않는 방식**(덧붙이기)으로 갑니다.

---

## 확인만 해주시면 좋은 것

- **약관·개인정보처리방침 내용** — 코드에서 실제 수집 항목을 뽑아 썼지만, 운영자 정보(이성현 / infinagree@gmail.com)와 "만 14세 미만 가입 불가" 방침이 맞는지 봐주세요
- **일반 회원에게 코치님 말씀을 안 여는 것** — 학교 자산이라 잠갔습니다. 교장쌤·코치 의견이 다르면 바꾸겠습니다
