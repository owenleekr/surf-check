# 카카오 로그인 붙이기 — 오웬이 해줄 것 / 내가 할 것

**결론부터: 가능합니다.** Supabase Auth가 카카오를 공식 지원합니다.
다만 **카카오 개발자 앱 등록은 오웬 계정으로만 가능**해서, 거기까지는 제가 못 합니다.

그리고 한 가지 미리 알아두실 게 있습니다 — **이메일은 사업자 등록(Biz App)을 해야 받을 수 있습니다.**
사업자 없이 붙이면 카카오가 주는 건 **닉네임 + 프로필 이미지**뿐입니다.
서핑 앱에는 그걸로 충분하니 굳이 Biz App까지 갈 필요는 없다고 봅니다.

---

## 1. 오웬이 해줄 것 (카카오 개발자 콘솔)

https://developers.kakao.com → 로그인 (오웬 카카오 계정)

### ① 앱 만들기
`내 애플리케이션` → `애플리케이션 추가하기`

| 칸 | 넣을 값 |
|---|---|
| 앱 아이콘 | `web/icon-booth.png` 같은 정사각 이미지 (없으면 나중에) |
| 앱 이름 | **라인업** |
| 회사명 | 개인이면 본인 이름 (**이성현**) |
| 카테고리 | 스포츠 |

### ② 플랫폼 등록
`앱 설정` → `플랫폼` → **Web 플랫폼 등록**

```
사이트 도메인:  https://surf.owenai.xyz
```

### ③ 카카오 로그인 켜기
`제품 설정` → `카카오 로그인` → **활성화 설정 ON**

그 다음 같은 화면의 **Redirect URI**에 아래를 넣습니다.
`<프로젝트ref>`는 Supabase 프로젝트 주소의 앞부분입니다(이 프로젝트는 `tneidzyhriefzubkajkv`).

```
https://tneidzyhriefzubkajkv.supabase.co/auth/v1/callback
```

> 이 주소는 **Supabase가 받는 자리**입니다. surf.owenai.xyz가 아니라 supabase.co인 게 맞습니다.

### ④ 동의항목
`제품 설정` → `카카오 로그인` → `동의항목`

| 항목 | 설정 |
|---|---|
| 닉네임 (`profile_nickname`) | **필수 동의** |
| 프로필 사진 (`profile_image`) | 선택 동의 |
| 카카오계정(이메일) (`account_email`) | **Biz App 전환 후에만 가능** — 지금은 건너뜁니다 |

### ⑤ 키 두 개 복사
`앱 설정` → `앱 키`

- **REST API 키** → Supabase에서 `client_id`로 씁니다
- `제품 설정` → `카카오 로그인` → **Client Secret** → **활성화 ON** 후 코드 복사

### ⑥ Supabase에 넣기
Supabase 대시보드 → `Authentication` → `Providers` → **Kakao**

- Enable Kakao: **ON**
- Client ID: ⑤의 **REST API 키**
- Client Secret: ⑤의 **Client Secret**
- (이메일 동의를 안 받으므로) `Authentication` → `Providers` → **Allow users without an email** 을 켜 두세요

> ⚠️ **REST API 키와 Client Secret은 저에게 보내지 마세요.** Supabase 콘솔에 직접 넣으시면 됩니다.
> 제가 쓰는 코드는 그 값을 몰라도 됩니다 — `supabase.auth.signInWithOAuth({ provider:'kakao' })` 한 줄이라서요.

---

## 2. 제가 할 것 (⑥까지 끝나면)

### 지금 구조와 합치는 문제
이 앱은 지금 **Supabase Auth를 안 씁니다.** 직접 만든 `accounts` 테이블 + bcrypt RPC입니다.
카카오를 붙이면 인증 경로가 둘이 되므로, 둘 중 하나로 갑니다.

| 방법 | 내용 | 위험 |
|---|---|---|
| **A. 카카오를 덧붙이기** (권장) | 카카오로 들어오면 `auth.users` 행이 생기고, 그걸 기존 `accounts` 행에 **연결**합니다. 기존 20명은 아이디·비번 그대로. | 낮음. 지금 로그인이 안 깨집니다 |
| B. 전부 Supabase Auth로 이전 | 깔끔하지만 기존 20명 계정을 옮기고 RLS를 다시 짜야 합니다 | 높음. 작업 중 로그인이 깨질 수 있습니다 |

**A를 권합니다.** 6기 20명이 이미 쓰고 있는 걸 건드리지 않는 게 먼저입니다.

### A로 갈 때 제가 하는 일
1. 로그인 화면에 **"카카오로 시작하기"** 버튼 추가 (노란 카카오 버튼 규격 준수)
2. 첫 카카오 로그인이면 → 닉네임을 받아 `accounts` 행을 만들고 `cohort='open'`으로 시작
3. 이미 아이디·비번 계정이 있는 사람은 **나 탭에서 "카카오 연결"** 로 한 계정에 묶기
4. 카카오 탈퇴/연결 해제 처리
5. 약관·개인정보처리방침에 **카카오에서 받는 정보(닉네임·프로필 사진)** 항목 추가
6. 카카오 로그인 화면·버튼이 [카카오 디자인 가이드](https://developers.kakao.com/docs/latest/ko/reference/design-guide)에 맞는지 확인

---

## 3. 미리 준비해 둔 것

카카오 앱 등록 심사에서 **개인정보처리방침 URL**을 요구합니다. 이미 만들어 뒀습니다.

```
이용약관        https://surf.owenai.xyz/terms-of-service.html
개인정보처리방침  https://surf.owenai.xyz/privacy.html
```

`앱 설정` → `앱 정보` 에 이 주소들을 넣으시면 됩니다.

---

## 4. 안 하셔도 되는 이유도 적어둡니다

지금 아이디·비밀번호 방식도 잘 돌아갑니다. 카카오를 붙이면 좋은 점은 **가입 이탈이 줄고
비밀번호 분실 문의가 사라진다**는 것이고, 드는 비용은 **위 ①~⑥ 설정 30분 + 제 작업**입니다.

6기 20명만 쓸 거면 굳이 필요 없습니다.
**일반 회원을 받아 커뮤니티를 키울 거면 붙이는 쪽이 확실히 낫습니다** — 한국에서 카카오 없는
가입 폼은 이탈률이 눈에 띄게 높습니다.
